<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="{{ absolute_url(asset('css/bootstrap.css')) }}" rel="stylesheet">
        <link href="{{ absolute_url(asset('css/pdf.css')) }}" rel="stylesheet" media="print">
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-xs-2 text-center"><img src="{{ absolute_url(asset('/images/logo.jpg')) }}" alt="UUFNN logo"></div>
                <div class="col-sm-8"><h3>{{ show.show }} Tickets</h3></div>
                <div class="col-sm-8"><h4>{{ artist.firstName~' '~artist.lastName }}</h4></div>
            </div>
        </div>
        <script src="{{ asset('js/bootstrap.js') }}"></script>
    </body>
</html>